name: Release candidate

on:
  workflow_dispatch:
    inputs:
      version:
        description: Semantic version without the v prefix
        required: true
        default: 1.10.0
        type: string

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  package:
    runs-on: windows-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Build, test and package
        shell: pwsh
        run: .\scripts\Package.ps1 -Version '${{ inputs.version }}'

      - name: Attest portable package
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373
        with:
          subject-path: artifacts/Raudo-v${{ inputs.version }}-win.zip

      - name: Attest direct executable
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373
        with:
          subject-path: artifacts/Raudo-v${{ inputs.version }}-win.exe

      - name: Upload candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: Raudo-v${{ inputs.version }}-win
          path: |
            artifacts/Raudo-v${{ inputs.version }}-win.zip
            artifacts/Raudo-v${{ inputs.version }}-win.zip.sha256
            artifacts/Raudo-v${{ inputs.version }}-win.exe
            artifacts/Raudo-v${{ inputs.version }}-win.exe.sha256
            artifacts/Raudo-win.exe
            artifacts/Raudo-win.exe.sha256
          if-no-files-found: error
          retention-days: 14
